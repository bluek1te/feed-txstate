# {{ ansible_managed }}
#
# nginx site to serve frontend assets

server {
#  listen 443 ssl http2;
  listen 80;
  server_name {{ frontend_domain }};

  include /etc/nginx/ssl.conf;

  root {{ app_root }}/hcfb-fe/dist/;
}

# server {
#   listen 80;
#   server_name {{ frontend_domain }};
#   return 301 https://$server_name$request_uri;
# }
